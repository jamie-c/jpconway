import StyledLink from "../StyledLink"

const PostLink = ({ title, url }: {title: string, url: string}) => {
    return (
        <div className="">
            <StyledLink title='' url='' />
        </div>
    )
}

export default PostLink